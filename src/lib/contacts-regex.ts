// Регулярка поиска контактов в описании проекта.
// ВАЖНО: подставляется в SQL как ПАРАМЕТР (`~* ${CONTACT_REGEX}`), а не
// захардкоживается внутрь sql-строки. Иначе Postgres теряет слэши (\d -> d)
// и запрос падает с "Failed query".
export const CONTACT_REGEX =
  "@[\\wа-яё]{3,}|t\\.me/|[\\w.+-]+@[\\w.-]+\\.[\\w]{2,}|whatsapp|ва?тсап|(\\+7|8)[\\s-]?\\(?\\d{3}\\)?[\\s-]?\\d{3}[\\s-]?\\d{2}[\\s-]?\\d{2}";
